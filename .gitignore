# ── Internal QA / pre-release docs (not for public distribution) ─────────────
docs/pre-release-checklist.md
docs/test-plan.md
docs/qa-review.md
docs/project-knowledge.md

# ── PDFs (e.g. CIS Benchmark — redistribute separately) ──────────────────────
*.pdf

# ── Test output ───────────────────────────────────────────────────────────────
tests/results/

# ── CIS audit tool runtime outputs ───────────────────────────────────────────
reports/
cis_checkpoints/
audit-debug.txt
debug-tb.txt
pester-fail.txt

# ── Local environment / secrets ───────────────────────────────────────────────
.env
.env.*
*.local.json
local.*.json

# ── Certificates and keys ─────────────────────────────────────────────────────
*.pfx
*.pem
*.key
*.p12
*.cer

# ── Azure CLI token cache ─────────────────────────────────────────────────────
.azure/

# ── PowerShell temp / log files ───────────────────────────────────────────────
*.log
*.tmp

# ── VS Code Local History extension ──────────────────────────────────────────
.history/

# ── Bicep compiled outputs (az bicep build) ───────────────────────────────────
*.compiled.json
bicep/**/*.json

# ── Azure deployment output / parameter snapshots ────────────────────────────
*.deploymentOutput.json
azuredeploy.json
azuredeploy.parameters.json

# ── Copilot prompt files (internal — not for public distribution) ─────────────
.github/prompts/

# ── OS and editor artefacts ───────────────────────────────────────────────────
Thumbs.db
.DS_Store
desktop.ini

# ── Office documents (should not be in repo root) ────────────────────────────
*.docx
*.xlsx
*.pptx
